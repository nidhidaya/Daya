CREATE TABLE EMP_REG 
   (	"ENO" NUMBER(10,0) NOT NULL ENABLE, 
	"ENAME" VARCHAR2(20 BYTE), 
	"EADD" VARCHAR2(20 BYTE), 
	"G_SAl" FLOAT(126), 
	"NET_SAL" FLOAT(126), 
	 CONSTRAINT "EMP_REG_PK" PRIMARY KEY ("ENO"));
	 ===========================================================
	 CREATE SEQUENCE  "SYSTEM"."ENO_SEQ1"  MINVALUE 1 MAXVALUE 10000 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE ;